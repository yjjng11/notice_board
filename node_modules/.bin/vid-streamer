#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../vid-streamer/bin/vid-streamer" "$@"
  ret=$?
else 
  node  "$basedir/../vid-streamer/bin/vid-streamer" "$@"
  ret=$?
fi
exit $ret
